---
export interface NavLinkProps {
  title: string;
  href: string;
}
const { title, href } = Astro.props as NavLinkProps;
---

<a
  href={href}
  class="text-tertiary font-helvetica text-center text-sm md:text-lg 3xl:text-xl hover:text-opacity-80 hover:scale-110"
  >{title}</a
>
